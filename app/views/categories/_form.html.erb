<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="row">
      <div class="col-lg-12 col-xl-12">
        <div class="form-group row">
          <%= form.label :name, class: "col-form-label col-12" %>
          <%= form.text_field :name, id: :category_name, class: "form-control col-12" %>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
    <div class="row">
      <div class="col-lg-12 col-xl-12">
        <div class="form-group row">
          <%= form.label :description, "Values", class: "col-form-label col-12" %>
          <%= form.text_field :description, id: :category_description, class: "form-control" %>
        </div>
      </div>
    </div>
  </div>
  <div class="actions">
    <%= form.submit :Submit, class: "btn btn-success" %>
  </div>
<% end %>
