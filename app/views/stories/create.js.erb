<% if @story.errors.any? %>
  alert("Can't Save! <%= @story_.errors.messages %>")
<%else%>
  form = "<%= escape_javascript(render partial: "story/index", story: @story )  %>";
  $("#storyIndex").html(form);
<%end%>
