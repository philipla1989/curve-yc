<!DOCTYPE html>
<html>
  <head>
    <% if (@title.present? && @description.present?) %>
      <title><%= @title %></title>
      <meta name="Description" content="<%= @description %>">
    <% else %>
      <title>CurveYourCareer</title>
      <meta name="Description" content="Find fulfillment and happiness in your job. Hear about how people just like you were able to change careers.">
    <% end %>
    <% if @story.present? %>
      <% if @story.meta_name.present? && @story.meta_content.present? %>
        <meta name="<%= @story.meta_name %>" content="<%= @story.meta_content %>">
      <% end %>
    <% end %>
    <% if @post.present? %>
      <% if @post.meta_name.present? && @post.meta_content.present? %>
        <meta name="<%= @post.meta_name %>" content="<%= @post.meta_content %>">
      <% end %>
    <% end %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Inconsolata" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= render "shared/utilities/bootstrap" %>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-111267241-1"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-111267241-1');
    </script>

    <script src="//load.sumome.com/" data-sumo-site-id="97ea7a43464d6f7917cf7ace472ad4f1fea1fb96391368f4afbc1cf30c904d1a" async="async"></script>

  </head>

  <body class="bodyFont" style="background-color:#1274d926;" data-turbolinks="false">

    <%= render 'layouts/navigation' %>

    <div class="container container-fluid" style="background-color: #ffffff;min-height:100vh;">
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
